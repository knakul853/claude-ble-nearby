{
  "mcpServers": {
    "ble-nearby": {
      "command": "bash",
      "args": ["-c", "cd \"${CLAUDE_PLUGIN_ROOT}\" && [ -d node_modules ] || npm install --production --silent && node dist/mcp-server.js"]
    }
  }
}
